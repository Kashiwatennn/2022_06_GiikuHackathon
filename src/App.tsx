import styled from "styled-components";

export const App = () => {
  return (
    <SContainer>
      <div>メロン組へようこそ！</div>
    </SContainer>
  );
};

const SContainer = styled.div`
  text-align: center;
`;
